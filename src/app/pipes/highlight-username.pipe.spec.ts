import { HighlightUsernamePipe } from './highlight-username.pipe';

describe('HighlightUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
